#!/usr/bin/env bash
# Automates the process of making API requests and saving the results to a file

# Makes the request to the API

# Save the response to a json file - 'data.json'

# If error fails, log the error to - 'errors.txt'
curl https://pokeapi.co/api/v2/pokemon > data.json
